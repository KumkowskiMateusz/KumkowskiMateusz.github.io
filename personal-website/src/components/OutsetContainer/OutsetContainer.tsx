import React from 'react'

interface OutsetContainerProps {
    content :  React.ReactNode
    color ?: string
}

const OutsetContainer = (_props : OutsetContainerProps) => {
  return (
    <div>OutsetContainer</div>
  )
}

export default OutsetContainer